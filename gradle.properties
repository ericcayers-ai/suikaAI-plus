org.gradle.jvmargs=-Xmx2g -XX:+UseZGC
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

# Versions
dyn4jVersion=5.0.2
libgdxVersion=1.12.1
imguiVersion=1.86.13
junitVersion=5.11.4
slf4jVersion=2.0.16
# Matches the LWJGL version gdx-backend-lwjgl3 1.12.1 pulls transitively — the
# RT Lab's raw Vulkan bindings must be the exact same LWJGL release as the rest
# of the app's LWJGL modules (mixing versions crashes at native-load time).
lwjglVersion=3.3.3
onnxruntimeVersion=1.20.0
