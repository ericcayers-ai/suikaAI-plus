# Runtime + training dependencies for the optional Python side (PPO, Decision Transformer,
# the Gymnasium env, and TensorBoard logging). The JVM game needs none of this — it is only
# required if you train in Python. Install with:  pip install -r requirements.txt
#
# The suika-game app installs an equivalent managed venv on demand from
# Settings -> AI ENVIRONMENT; this file exists for reproducible manual/CI setups.

numpy>=1.24.0
gymnasium>=1.3.0
stable-baselines3>=2.3.0
torch>=2.1.0
tensorboard>=2.14.0

# Dev / lint (also available via: pip install -e .[dev])
ruff>=0.4.0
pytest>=7.0.0
