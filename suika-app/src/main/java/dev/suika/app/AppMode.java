package dev.suika.app;

/**
 * The two UX tiers described in ROADMAP §VII.1.
 * Explorer mode hides advanced knobs behind friendly presets;
 * Researcher mode exposes the full algorithm zoo and every hyperparameter.
 */
public enum AppMode {
    EXPLORER,
    RESEARCHER
}
